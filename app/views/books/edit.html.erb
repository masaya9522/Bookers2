<main>
  
  <div class="container">
    <div class="row">
      <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
        
        <% if @show_book.errors.any? %>
        <%= @show_book.errors.count %>errors prohibited this book from being saved:
        <ul>
          <% @show_book.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
        <% end %>

      <h1>Editing Book</h1>

<%= form_with model: @show_book, url: book_path(@show_book.id), method: :patch do |f| %>


      <div class="form-group">
        <h4>Title</h4>
          <%= f.text_field :title %>
      </div>
  
      <div class="form-group">
        <h4>Opinion</h4>
          <%= f.text_area :body %>
      </div>
  
      <div class="form-group">
        <%= f.submit 'Update Book' %>
      </div>  
  
<% end %>

<%= link_to book_path(@show_book.id) do %>Show<% end %>
|
<%= link_to "Back", books_path%>

      </div>
    </div>
  </div>

